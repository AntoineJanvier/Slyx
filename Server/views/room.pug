doctype html
html
    head
        title rtc.io simple conferencing
        link(rel='stylesheet' href='http://yui.yahooapis.com/pure/0.5.0/pure-min.css')
    body(onload="RTC()" class="pure-g")
        div.pure-u-1-5
            div#l-video
        div.pure-u-4-5#r-video
        script(src="https://cdn.jsdelivr.net/rtc/latest/rtc.min.js")
        - rtc = RTC({room: 'room/' + json.from + '/' + json.to + '/',signaller: 'https://switchboard.rtc.io/'});
//        - var localVideo = document.getElementById('l-video');
//        - var remoteVideo = document.getElementById('r-video');
//        - localVideo.appendChild(rtc.local);
//        - remoteVideo.appendChild(rtc.remote);
